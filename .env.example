# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Authentication Configuration - Environment-aware URL resolution
# NEXTAUTH_URL will be auto-resolved based on environment detection
# Only set this if you need to override automatic detection:
# - Development: auto-detects localhost:3000 (or PORT)
# - Staging/Production: use NEXT_PUBLIC_APP_URL or VERCEL_URL
# NEXTAUTH_URL=your-override-url-if-needed
NEXTAUTH_SECRET=your-secret-key

# OAuth Provider Configuration
# Google OAuth (Production Configured)
# Configured with Google Cloud Console project
# Client ID: 455260463368-lcokr50o1cvtsjs9m7pmujjnkvmc4756.apps.googleusercontent.com
# Authorized redirect URIs configured for production domain
GOOGLE_CLIENT_ID=455260463368-lcokr50o1cvtsjs9m7pmujjnkvmc4756.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Supabase Google OAuth Configuration
# In Supabase Dashboard → Authentication → Providers → Google:
# 1. Google provider is enabled
# 2. Client ID and Secret configured
# 3. Redirect URLs configured for production

# Google Maps API Key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIza-your-google-maps-api-key

# Email Configuration (Production Ready)
EMAIL_FROM=people@kuchisabishii.io
SMTP_HOST=smtp.mail.me.com
SMTP_PORT=587
SMTP_USER=people@kuchisabishii.io
SMTP_PASSWORD=meha-dyvx-rvuk-hrys

# Security Configuration
JWT_SECRET=your-jwt-secret-key
ENCRYPTION_KEY=your-32-character-encryption-key

# Rate Limiting Configuration
REDIS_URL=redis://localhost:6379

# Logging Configuration
LOG_LEVEL=info
SENTRY_DSN=your-sentry-dsn-for-error-tracking
